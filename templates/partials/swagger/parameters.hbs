{{#if parameters}}
  <h4>Parameters</h4>
  {{#each parameters}}
    {{#if $ref}}
      {{> swagger/parameterRow parameter=(json-schema--resolve-ref $ref) $ref=$ref}}
    {{else}}
      {{> swagger/parameterRow parameter=.}}
    {{/if}}
  {{/each}}
{{/if}}
